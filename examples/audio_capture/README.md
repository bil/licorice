Before running this example, make sure to read [the README for the `sine` example](../sine/README.md) and verify that that works.

To add a virtual loopback device, run:
```bash
sudo modprobe snd_aloop
```
