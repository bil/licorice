# place in ~/.xinitrc
#
# This example xinitrc launches openbox and a single terminal.
# The X server will stop if the terminal is closed.
#
# Note: Upon first launch, it may appear that no terminal is present, but hitting any button will bring the terminal into view.

openbox &
lxterminal
