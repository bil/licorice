# __DRIVER_CODE__ imports
import parallel

# __DRIVER_CODE__ variables

# __DRIVER_CODE__ setup

# make sure parport is writeable
pport = parallel.Parallel(port={{out_signal['args']['addr']}})

# __DRIVER_CODE__ write

  pport.setData(<unsigned char>outBuf[0])

# __DRIVER_CODE__ exit_handler
  free(outBuf)
